FROM n8nio/n8n:latest

USER root

# Install any extra dependencies if needed (e.g. for custom scripts)
# RUN apk add --no-cache ...

USER node

# Default port for n8n is 5678, but we can override it via env vars in Railway
EXPOSE 5678
